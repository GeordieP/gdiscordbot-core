"use strict"

module.exports = {
    // where the platform core plugins (bundled plugins) should be loaded from
    core_plugins_path: "./core_plugins/",

    // where the user-installed plugins should be loaded from
    user_plugins_path: "./plugins/"
}
